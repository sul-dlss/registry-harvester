#!/bin/bash

. /s/SUL/Config/sirsi.env

HOME=/s/SUL/Harvester
LOG=$HOME/log
OUT=$HOME/out
JVM1=$1
JVM2=$2
OUTFile=$3
DATE=`date +%Y%m%d%H%M`

if [ -z $JVM1 ]
then
  JVM1="2G"
fi

if [ -z $JVM2 ]
then
  JVM2="10G"
fi

if [ "$OUTFile" == "latest" ]
then
  for F in `ls $OUT/course_harvest.out* | tail -1`
  do
    OUTFile="$F"
  done
elif [ -z $OUTFile ]
then
  for F in `ls $OUT/course_harvest.out*`
  do
    OUTFile="$OUTFile $F"
  done
fi

cp $HOME/include/courses/*.xml $HOME/include/courses/LastRun/

cd $HOME/classes/course

echo "Running: java -Xms${JVM1} -Xmx${JVM2} -cp .:../../lib/jdom-2.0.6.jar:../shared/PropGet.class BuildCourseXML $OUTFile"

java -Xms${JVM1} -Xmx${JVM2} -cp .:../../lib/jdom-2.0.6.jar:../shared/PropGet.class BuildCourseXML $OUTFile

echo "Copied courseXML files to /s/SUL/Dataload/CourseReserves" >> $LOG/course_build.log
cp $HOME/include/courses/courseXML_*.xml /s/SUL/Dataload/CourseReserves/

echo "" >> $LOG/course_build.log
echo "New files generated:" >> $LOG/course_build.log
ls -lS $HOME/include/courses/*.xml >> $LOG/course_build.log

echo "" >> $LOG/course_build.log
echo "Last Run:" >> $LOG/course_build.log
ls -lS $HOME/include/courses/LastRun/*.xml >> $LOG/course_build.log

cat $LOG/course_build.log | mailx -s 'Course Build Log' sul-unicorn-devs@lists.stanford.edu

mv $LOG/course_build.log $LOG/course_build.log.$DATE
touch $LOG/course_build.log
